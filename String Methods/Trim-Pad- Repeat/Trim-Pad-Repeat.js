"  hello  ".trim()           // "hello"      — dono taraf spaces hatao
"  hello  ".trimStart()      // "hello  "    — sirf left
"  hello  ".trimEnd()        // "  hello"    — sirf right

"5".padStart(4, "0")         // "0005"       — left se pad karo
"5".padEnd(4, "0")           // "5000"       — right se pad karo

"Ha".repeat(3)               // "HaHaHa"